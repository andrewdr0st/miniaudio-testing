#ifndef MATH_UTILS_H
#define MATH_UTILS_H

#define ONE_OVER_127 0.007874f

#define LERP(a, b, t) ((a) + (t) * ((b) - (a)))
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#endif